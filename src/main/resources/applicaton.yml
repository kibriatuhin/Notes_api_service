spring:
  application:
    name: notes-api-service
  profile:
    active: dev
---
#dev configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/enotes_dev
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: none
  mail:
    host: smtp.gmail.com
    username: tgolamkibria@gmail.com
    password: usdk szbx hemc rxpg
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
email:
  send:
    from: tgolamkibria@gmail.com
file:
  upload:
    path: notesFile/

#spring.jpa.hibernate.ddl-auto=none
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect